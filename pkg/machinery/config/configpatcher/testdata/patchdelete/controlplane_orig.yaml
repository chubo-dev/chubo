version: v1alpha1
debug: false
persist: true
machine:
  type: controlplane
  token: d8cwfa.eyvpi0xwxyarbfid
  certSANs: []
  files:
    - content: keep=1
      permissions: 0o644
      path: /etc/chubo/keep.conf
      op: overwrite
    - content: delete=1
      permissions: 0o600
      path: /etc/chubo/delete.conf
      op: overwrite
  sysctls:
    net.ipv4.ip_forward: "1"
    vm.swappiness: "10"
cluster:
  clusterName: base
  discovery:
    enabled: true
    registries:
      service:
        endpoint: https://discovery.example.com
