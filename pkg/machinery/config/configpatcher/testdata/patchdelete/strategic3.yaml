version: v1alpha1
machine:
  files:
    - path: /etc/chubo/temp.conf
      content: temp=1
      permissions: 0o644
      op: overwrite
cluster:
  discovery:
    registries:
      service:
        endpoint:
          $patch: delete
