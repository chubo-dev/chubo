version: v1alpha1
machine:
  network:
    hostname: hostname-foo
cluster:
  clusterName: base
  discovery:
    enabled: true
    registries:
      kubernetes: {}
      service:
        endpoint: https://discovery.example.com/
