version: v1alpha1
machine:
  network:
    hostname: hostname-foo
cluster:
  clusterName: base
  discovery:
    enabled: true
    registries:
      service:
        endpoint: https://discovery.example.com/
