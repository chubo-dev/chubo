debug: false
machine:
  files:
    - content: right=1
      permissions: 0o600
      path: /etc/chubo/right.conf
      op: overwrite
  sysctls:
    net.ipv4.ip_forward: "0"
    vm.swappiness: "10"
cluster:
  clusterName: right
  discovery:
    registries:
      service:
        endpoint: https://discovery.right
